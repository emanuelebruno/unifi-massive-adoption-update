# Legacy TRAE project rules

This file is retained as historical context only. It is non-normative and must not be used as the current operating contract.

The repository was originally limited to Ubiquiti UniFi UAP-IW / U2IW preparation. The project now has a vendor-neutral AP lifecycle-management direction, while its current implementation and modifying eligibility remain explicitly limited as documented in `AGENTS.md`.

`AGENTS.md` is authoritative for scope, phase status, safety gates, firmware policy, verification, and implementation workflow. If this legacy file, prior TRAE instructions, or historical prompts conflict with `AGENTS.md`, follow `AGENTS.md` unless the human explicitly instructs otherwise for a particular task.
